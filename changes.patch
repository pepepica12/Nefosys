--- a/package.json
+++ b/package.json
@@ -1,6 +1,15 @@
 {
   "name": "nefosys",
   "private": true,
+  "engines": {
+    "node": ">=19"
+  },
   "scripts": {
     "dev": "next dev",
     "build": "next build",
     "start": "next start",
+    "db:migrate": "drizzle-kit migrate",
+    "db:generate": "drizzle-kit generate",
+    "db:push": "drizzle-kit push",
+    "db:studio": "drizzle-kit studio"
   },
   "dependencies": {
+    "drizzle-orm": "0.44.2",
+    "@neondatabase/serverless": "1.0.1"
   },
   "devDependencies": {
+    "drizzle-kit": "0.31.4"
   }
 }
--- a/vercel.json
+++ b/vercel.json
@@ -1,6 +1,12 @@
 {
   "build": {
     "env": {
-      "POSTGRES_URL": "@DATABASE_URL"
+      "DATABASE_URL": "@DATABASE_URL"
     }
   }
 }
--- a/.env
+++ b/.env
@@ -1,6 +1,10 @@
-POSTGRES_URL=...
+# To keep your Neon database permanently, claim it here: <CLAIM_URL_IF_PROVIDED>
+DATABASE_URL=...
--- a/drizzle.config.ts
+++ b/drizzle.config.ts
@@ -1,6 +1,14 @@
 import type { Config } from "drizzle-kit";
 
 export default {
   schema: "./src/schema.ts",
-  out: "./drizzle",
-  driver: "pg"
+  out: "./drizzle",
+  driver: "pg",
+  dbCredentials: {
+    connectionString: process.env.DATABASE_URL as string
+  },
+  strict: true,
+  verbose: true
 } satisfies Config;
--- a/src/repository/todos.ts
+++ b/src/repository/todos.ts
@@ -1,10 +1,14 @@
-import { sql } from "drizzle-orm";
+import { sql } from "../db";
 import { eq } from "drizzle-orm";
 import { todos } from "../schema";
 
 export async function getTodoById(id: string) {
-  return await sql("SELECT * FROM todos WHERE id = $1", [id]);
+  const result = await sql`SELECT * FROM ${todos} WHERE ${todos.id} = ${id}`;
+  return result;
 }
 
 export async function listTodos() {
-  return await sql("SELECT * FROM todos");
+  const result = await sql`SELECT * FROM ${todos}`;
+  return result;
 }
